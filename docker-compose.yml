version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: burger
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Fla85192502
    ports:
      - "5432:5432"